[project]
name = "aitu-multi-agent-systems"
version = "0.1.0"
description = "Anomaly Detection for Network Security using NSL-KDD dataset"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "torchvision",
    "pandas",
    "numpy",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "jupyterlab",
    "ipykernel",
    "tqdm",
    "pyyaml",
    "jinja2",
    "md2pdf",
]
